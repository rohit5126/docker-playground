name: pr-pipeline
on:
  pull_request:
    branches: [master]
    types: [opened, synchronize]
  workflow_dispatch:
  push:
    

jobs:
  job1:
    uses: ./.github/workflows/reusable-build-test.yml
    with:
      node-version: '22' 
      package-path: 'frontend' 
      run_tests: false
      
    
    
        
